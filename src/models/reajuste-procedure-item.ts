export class ReajusteProcedureItem {
  title: string;
  text: string;
  icon: string;

  constructor(title: string, text: string, icon: string) {
    this.title = title;
    this.text = text;
    this.icon = icon;
  }
}
